#include<iostream>

using namespace std;

int main(){

    int a=1;

    // if(if(a==1)){
            //    it is wrong because in if statement we only give condtion
    // }

    // if(func1 (a)){
        
    // }

    if(char(a)){
        cout<<(a);
    }
}